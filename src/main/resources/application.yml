spring.application.name=sns_backend
spring:
  application:
    name: sns_backend
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration
